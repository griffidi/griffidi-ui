{
  "name": "workspace",
  "description": "A full-stack workspace with core and component libraries, an GraphQL API project and a React demo app",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@10.9.0",
  "engines": {
    "node": ">=23.11.0"
  },
  "type": "module",
  "scripts": {
    "clear": "pnpm recursive exec rm -rf node_modules && rm -rf dist",
    "setup": "pnpm install",
    "lint": "pnpx biome lint"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.0-beta.1",
    "@types/node": "latest",
    "esbuild": "0.25.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
